// swift-tools-version:5.2

import PackageDescription

let package = Package(
    name: "SpreadsheetView",
    products: [
        .library(
            name: "SpreadsheetView",
            targets: ["SpreadsheetView"]),
    ],
    dependencies: [],
    targets: [
        .target(
            name: "SpreadsheetView",
            dependencies: [])
    ]
)
